# Batch 007 — Immutable Printed First-Layer Recipes

Base commit: `79f36da`

Batch 007 freezes the accepted variable first-layer coupon as separate PLA+ and TPU object recipes.

## Added

- Grid Stack API version 2 for primitive first-layer recipes.
- A trace-specific process record without structural pass or bridge fields.
- A printer-hardware record with an explicit `unrecorded` state.
- Source commit storage inside each saved object.
- Immutable PLA+ and TPU printed-object files.
- Lesson 007 and a printed-object manifest.

## Deliberately delayed

The twelve planned structural span/gap coupon files are not generated yet. Their permanent recipes should be created only after printable structural-strand and vertical-stack geometry exists. Saving diagnostic-only placeholders would pin them to the wrong execution contract.
